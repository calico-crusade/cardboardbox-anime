import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cardboardbox.anime',
  appName: 'Cardboard Box',
  webDir: 'dist/cardboard-box-anime',
  bundledWebRuntime: false
};

export default config;
